// Deprecated - switching via drag is removed in favor of click-to-set.
// Keep a no-op export to avoid runtime import errors if referenced.
export function setupSwitchDragHandlers(_hook) {
  // No-op
}
